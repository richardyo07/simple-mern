/** @format */
import React from 'react';

const PageHeader = ({ titleText }) => (
  <div className="row">
    <div className="col-12">
      <h1>{titleText}</h1>
    </div>
  </div>
);

export default PageHeader;
